# Default: normalize text, store LF in repo
* text=auto eol=lf

# Unreal Engine binary assets via Git LFS (lockable is helpful in teams)
*.uasset filter=lfs diff=lfs merge=lfs -text lockable
*.umap   filter=lfs diff=lfs merge=lfs -text lockable

# Common large binaries also via LFS
*.fbx    filter=lfs diff=lfs merge=lfs -text
*.wav    filter=lfs diff=lfs merge=lfs -text

# Images as normal Git (not LFS), but treat as binary to avoid text diffs
*.png  binary
*.jpg  binary
*.jpeg binary

# Explicitly text (optional clarity)
*.uproject text
*.uplugin  text
*.ini      text
*.txt      text
*.md       text
*.json     text
*.bat      text
*.ps1      text
*.cpp      text
*.h        text

# Ensure repo-local hooks stay LF
.githooks/* text eol=lf
